import { StyleSheet } from "react-native";
import Colors from "../../constant/Colors"
const addNewCourseStyle = StyleSheet.create({
    error:{
        color: Colors.red,
        fontFamily: "outfit",
        fontSize: 14,
        marginTop: 10,
        marginLeft: 20
    }
})
export default addNewCourseStyle;